mutation RespondToAbsenceRequest(
  $eventId: Int!
  $email: String!
  $approved: Boolean!
) {
  respondToAbsenceRequest(
    eventId: $eventId
    email: $email
    approved: $approved
  ) {
    reason
  }
}
