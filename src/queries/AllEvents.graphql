query AllEvents {
  events {
    id
    name
    type
    callTime {
      date
      time
    }
    releaseTime {
      date
      time
    }
    userAttendance {
      shouldAttend
      didAttend
      confirmed
      minutesLate
    }
  }
}
